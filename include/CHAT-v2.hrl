%% Generated by the Erlang ASN.1 compiler. Version: 5.0.21.1
%% Purpose: Erlang record definitions for each named and unnamed
%% SEQUENCE and SET, and macro definitions for each value
%% definition in module CHAT-v2.

-ifndef(_CHAT_V2_HRL_).
-define(_CHAT_V2_HRL_, true).

-record('Feature', {
  id,
  key,
  value,
  group
}).

-record('Authority', {
  vsn,
  session,
  id,
  type,
  cert,
  settings
}).

-record('File', {
  id,
  mime,
  payload,
  parentid
}).

-record('Message', {
  vsn,
  session,
  id,
  from,
  to,
  files,
  type,
  link,
  seenby,
  repliedby,
  mentioned,
  status = asn1_DEFAULT
}).

-record('Privatebox', {
  address
}).

-record('Streambox', {
  channel
}).

-record('Groupbox', {
  channel
}).

-record('Mailbox', {
  channel
}).

-record('Inbox', {
  vsn,
  session,
  id,
  type,
  feed,
  messages
}).

-record('Ack', {
  vsn,
  id,
  primary,
  secondary = asn1_NOVALUE
}).

-record('Activity', {
  vsn,
  session,
  id,
  nickname,
  comments
}).

-record('Search', {
  vsn,
  session,
  id,
  dn,
  field,
  value,
  criteria,
  type,
  status
}).

-record('Subscription', {
  vsn,
  session,
  id,
  nickname,
  friend,
  status,
  type
}).

-record('Person', {
  nickname,
  avatar,
  names,
  phone,
  surnames,
  lastmsg,
  presence,
  update,
  created,
  settings,
  status
}).

-record('Server', {
  service,
  port,
  host
}).

-record('Roster', {
  vsn,
  session,
  id,
  nickname,
  keys,
  people,
  topics,
  channels,
  servers,
  update,
  status
}).

-record('Member', {
  nickname,
  feed,
  feeds,
  phone,
  avatar,
  names,
  surnames,
  alias,
  update,
  settings,
  presence,
  status
}).

-record('Conference', {
  vsn,
  session,
  id,
  name,
  links,
  description,
  settings,
  members,
  admins,
  data,
  type,
  tos,
  unread,
  mentions,
  lastmsg,
  update,
  created,
  status
}).

-record('CHATMessage', {
  no,
  headers,
  body
}).

-endif. %% _CHAT_V2_HRL_
